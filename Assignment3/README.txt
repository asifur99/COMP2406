“Life would be tragic if it weren’t funny." - after seeing my work

All you need to do is npm install package.json it'll install all the modules needed. 
Then you just start the server node server.js from shell and you will see it at localhost:3000

Default homepage is the greetings one and i have put the hrefs in header and footer and used templates which is kept in source folder.